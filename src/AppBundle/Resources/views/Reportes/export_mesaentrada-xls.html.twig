<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="application/vnd.ms-excel; charset=UTF-8" />
   </head>
   <style>
     .table-resumen{
        font-size:13px;
    }
    .table-resumen td:not(:first-child),.table-resumen th:not(:first-child){
        text-align: right;
        padding:2px 10px 2px 2px;
        vertical-align: middle;
    }
    .tipoS td{
        font-weight: bold;
    }
    .tipoS{
        background-color: antiquewhite;
        font-size: 15px;
        text-transform: uppercase;
    }
    .tipoM td:first-child{
        padding-left: 15px;
    }
    .tipoM{        
        font-size: 14px;
    }   
    .header{
        font-size: 14px;
        font-weight: bold;
        background-color: lightgray;
    }
    .tipoT{
        font-size: 14px;
        font-weight: bold;
        background-color: lightgray;
    }
   </style>
   <body  >     
      <table>
          <tr>
              <th>REPORTE DE MESA DE ENTRADA</th>
          </tr>
        </table>  
        <table>  
            <tr><td>Ubicaciones: {{ filtro.ubicaciones|trim }}</td></tr>
            <tr><td>Edificios: {{ filtro.edificios|trim }} </td></tr>
            <tr><td>Departamentos: {{ filtro.departamentos|trim }} </td></tr>
            <tr><td>Tipos de Insumos: {{ filtro.tiposInsumos}}</td></tr>
            <tr><td>Período: {{ filtro.desde}} - {{ filtro.hasta }}</td></tr>
            </tr>
        </table>  
            
        <table class="table-resumen">
                <tr class="header">
                    <th> Tipo de Equipo > Sector</th>
                    {% for mes in datos.labels %}
                     <th> {{ mes }} </th> 
                    {% endfor %}
                    <th> TOTAL </th>                        
                </tr>  
                {% for item in datos.tabla %}
                    <tr id="{{item.id}}" class="tipo{{item.tipo}}">
                        <td>{{ item.nombre }} </td>
                        {% for mes in datos.meses %}
                            <td> {{ item.recuento[mes] }} </td> 
                        {% endfor %}
                        <td>  {{ item.total }}   </td>                            
                    </tr>
                {% endfor %}
            </table>    

   </body>
</html>